# Database Configuration
# Copy this file to .env.local and fill in your values

# Supabase Database URLs
# Get these from your Supabase project settings -> Database -> Connection String

# Connection pooling URL (for serverless functions)
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[YOUR-PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true"

# Direct connection URL (for migrations)
DIRECT_URL="postgresql://postgres:[YOUR-PASSWORD]@[YOUR-PROJECT-REF].supabase.co:5432/postgres"

# Alternative: Local PostgreSQL
# DATABASE_URL="postgresql://postgres:password@localhost:5432/dashboard"
# DIRECT_URL="postgresql://postgres:password@localhost:5432/dashboard"

# Supabase Configuration (Optional - if using Supabase client)
NEXT_PUBLIC_SUPABASE_URL="https://[YOUR-PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[YOUR-ANON-KEY]"
